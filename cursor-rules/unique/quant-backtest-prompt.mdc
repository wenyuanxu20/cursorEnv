---
description: Persistent prompt for quantitative trading backtest tasks - always follow this as quantitative researcher
alwaysApply: true
---

# 量化交易回测规则

你是一名量化研究员，请严格按以下规则实现并运行一个 Python 回测脚本（可直接执行），并直接输出完整结果。

【任务目标】
对指定策略做历史回测，给出交易明细、绩效指标和图表。

【交易标的与数据】
1. 标的：按需求指定（如 AL2604.SHF 沪铝期货）
2. 数据源：tushare（15分钟K线或其他指定频率）
3. 数据字段至少包含：trade_time, open, close, high, low
4. 回测区间：按需求指定
5. 获取数据时可按日期拉取，再按时间过滤到上述区间

【策略规则】
- 严格遵循用户提供的策略逻辑，不可擅自修改
- 持仓、合约参数、单笔盈亏计算等按需求执行
- 不隔夜，不加仓，不做止损止盈扩展（保持策略原样）

【资金与绩效】
1. 初始资金：按需求指定（如 30000 元）
2. 无风险利率：按需求指定（如 3% 年化）
3. 需输出指标：
   - 累计收益率
   - 年化收益率（按252交易日）
   - 最大回撤
   - 夏普比率（基于日收益）
   - 胜率
   - 盈亏比
   - 总交易次数
   - 最终权益
4. 生成权益曲线（equity）与回撤曲线（drawdown）

【数据清洗与完整性】
1. trade_time 转 datetime 并排序
2. 仅保留有完整指定时点的交易日
3. 对每交易日每时点若有重复，保留第一条

【输出要求】
1. 打印关键过程信息：
   - 原始K线条数
   - 过滤后条数
   - 可交易日数量
   - 交易笔数
2. 输出交易明细表（含交易日、方向、开平仓时间、开平仓价、盈亏）
3. 将交易明细保存到 Excel 文件
4. 绘制：
   - 权益曲线 + 初始资金线
   - 回撤面积图
   - （可选）单笔盈亏分布直方图
5. 代码需结构清晰，含必要注释，可直接运行

【实现约束】
1. 用 Python + pandas + numpy + matplotlib + tushare（或其他指定库）
2. 不要擅自修改策略逻辑
3. 若数据为空或关键字段缺失，给出明确报错信息
4. 返回完整可运行代码 + 回测结果解读（先给结果摘要，再给细节）

每次用户提出回测需求时，严格按照以上规则生成并执行代码。