window.RULES_SOURCE = {
    "firecrawl-web-fetch": "---\ndescription: Prefer locally deployed Firecrawl + TrendRadar + Scrapling when fetching from the web\nalwaysApply: true\n---\n\n# Firecrawl + TrendRadar + Scrapling + agents-radar 优先联网取数（all projects）\n\n三件套都在 `cursorEnv/getInfo/` **本机已部署**，用户 `mcp.json` 已有对应键。**不要**等用户说「用 firecrawl / trendradar / scrapling」。对**每个工作区**生效。\n\n| 服务 | 本机部署 | 调用 |\n|------|----------|------|\n| Firecrawl | `getInfo/firecrawl/` API `http://127.0.0.1:3002`（自托管，默认无鉴权） | MCP 键 `firecrawl` / `user-firecrawl` |\n| TrendRadar | `getInfo/TrendRadar/` · Web `:8080` | Cursor：STDIO `getInfo/scripts/trendradar-mcp-stdio.py`（键 `trendradar`）；脚本：HTTP `:3333/mcp` |\n| Scrapling | `getInfo/scrapling/` 钉选 `scrapling[all]==0.4.15` | STDIO `getInfo/scrapling/.venv/Scripts/scrapling-mcp.exe`（键 `scrapling` / `user-scrapling`）；可选 HTTP `:3344` |\n| agents-radar | `getInfo/agents-radar/` HTTP `http://127.0.0.1:3355` | `start-agents-radar.ps1`；AstrBot `/radar_bind` |\n\n## 路由（先选工具，再抓）\n\n1. **热榜 / 舆情 / 今日新闻 / 多平台热点** → TrendRadar：`get_latest_news`、`search_news`、`get_trending_topics`；本地无今日数据时 `trigger_crawl`\n2. **AI CLI / OpenClaw / Agent 生态 / 海外 AI 工具日报** → agents-radar：`GET http://127.0.0.1:3355/card` 或 `/latest?type=ai-cli`；未起则 `start-agents-radar.ps1`\n3. **具体 URL / 文档站 / 任意网页正文** → Firecrawl：`firecrawl_scrape`；整站 `firecrawl_crawl`；链图 `firecrawl_map`\n4. **反爬 / Cloudflare / JS 重渲染 / Firecrawl 失败** → **本机 Scrapling**（不要改走 Cloud）：先 `make_request`；不够再 `fetch`；仍被拦再用 `stealthy_fetch`。MCP 不可用时 `getInfo/scripts/scrapling-scrape.ps1`\n5. **热点条目要全文** → TrendRadar 拿标题和 URL，再用 Firecrawl scrape；Firecrawl 失败再 Scrapling（不要默认用 TrendRadar `read_article`）\n6. **开放「网上搜」** → 先 TrendRadar；不够再试 `firecrawl_search`；自托管无 search 时 WebSearch **只拿 URL**，再用 Firecrawl scrape，失败再本机 Scrapling\n\n## 禁止默认走的路径\n\n- 不要先用 Cursor `WebFetch` / `WebSearch` 当主抓取器\n- 不要默认打 Firecrawl Cloud（`api.firecrawl.dev`）\n- 不要把 API Key / `.env` 写进回复或 Git\n- 不要默认用 Scrapling 浏览器去抓普通文档站（比 Firecrawl scrape 更慢、更重）\n\n## 服务未起\n\n- Firecrawl：`GET http://127.0.0.1:3002/v0/health/readiness` → `getInfo/scripts/start-firecrawl.ps1`\n- TrendRadar：STDIO 包装脚本或 `:3333` 不通 → `getInfo/scripts/start-trendradar.ps1`\n- Scrapling：`getInfo/scrapling/.venv/Scripts/scrapling-mcp.exe` 缺失 → `getInfo/scripts/start-scrapling.ps1`\n- agents-radar：`GET http://127.0.0.1:3355/health` → `getInfo/scripts/start-agents-radar.ps1`\n\nMCP 不可用时：Firecrawl 用 `getInfo/scripts/firecrawl-scrape.ps1 -Url <url>`；Scrapling 用 `getInfo/scripts/scrapling-scrape.ps1 -Url <url>`。仅当 **Firecrawl 与 Scrapling 都抓不到该 URL**（热榜场景还要 TrendRadar 也失败）才回退 WebFetch/WebSearch，并写明原因。\n\n## 回复必须点名工具\n\n实际调用了 Firecrawl / TrendRadar / Scrapling / agents-radar（或其它 `getInfo/` 新增取数工具）后，给用户的回复里**写明用了哪些**，不要只给结论。回退 `WebFetch` / `WebSearch` 同样点名。\n\n```text\n来源：TrendRadar `search_news` → Firecrawl `firecrawl_scrape` → Scrapling `make_request`\n```\n\n## AstrBot\n\n经 AstrBot 查网上信息时同样走本机 getInfo，不要改走 AstrBot 内置 Firecrawl Cloud / Tavily。\n\n| 路径 | 怎么调 getInfo |\n|------|----------------|\n| Cursor 主模型 | cursor-proxy → `agent --approve-mcps`；工作区 `ai/AstrBot` 用本规则 + `.cursor/mcp.json` |\n| SiliconFlow 回退 | 插件 `astrbot_plugin_getinfo` HTTP 调 `:3002` / `:3333`（不依赖 Cursor MCP） |\n| agents-radar 推送 | 插件 `astrbot_plugin_agents_radar` HTTP 调 `:3355`；`/radar_bind` |\n| 安装 | `getInfo/scripts/install-astrbot-getinfo.ps1`；`install-astrbot-agents-radar.ps1` |\n\n阿里云 ECS（2026-08-31 已部署）：TrendRadar `:3333` + scrape shim `:3002` + agents-radar `:3355`（无 Firecrawl Compose，机器 3.5 GiB）。`127.0.0.1` 是云主机本机。不要把 3002/3333/3355 放进安全组。\n",
    "llm-wiki-bootstrap": "---\r\ndescription: 中文触发“构建LLM wiki”自动搭建知识库\r\nalwaysApply: true\r\n---\r\n\r\n# 中文 LLM Wiki 自动构建规则\r\n\r\n## 触发词\r\n\r\n当用户输入以下任一短语时，立即按本规则执行，不先给方案：\r\n- `构建LLM wiki`\r\n- `构建 LLM wiki`\r\n- `搭建LLM wiki`\r\n- `建立LLM wiki知识库`\r\n- `build llm wiki`\r\n\r\n## 必做动作\r\n\r\n1. 在项目根目录创建（如不存在）：\r\n   - `raw/`\r\n   - `wiki/`\r\n   - `schema/`\r\n   - `wiki/index.md`\r\n   - `wiki/log.md`\r\n   - `AGENTS.md`\r\n\r\n2. 初始化模板内容：\r\n   - `AGENTS.md`：定义 ingest/query/lint、来源引用、命名规范、冲突处理、更新策略。\r\n   - `wiki/index.md`：按主题分类的页面目录模板。\r\n   - `wiki/log.md`：按时间记录 ingest 与页面变更模板。\r\n\r\n3. 保持幂等：\r\n   - 不覆盖用户已有非空内容。\r\n   - 文件已存在时，仅补充缺失章节；必要时新增 `*-template.md`。\r\n\r\n4. 完成脚手架后执行首轮：\r\n   - 若 `raw/` 已有资料：执行一次 ingest，更新相关页面并记入 `wiki/log.md`。\r\n   - 若 `raw/` 为空：向用户索要第一批资料或建议可导入来源。\r\n\r\n## 输出格式\r\n\r\n执行后必须输出：\r\n- 创建/更新文件清单\r\n- 一条可复用的 ingest 提示词\r\n- query 与每周 lint 的下一步清单\r\n",
    "agency-agent-router-beginner": "---\r\ndescription: 新手优先的 agency agent 自动路由（可手动覆盖）\r\nalwaysApply: true\r\n---\r\n\r\n# Agency Agent Auto Router (Beginner)\r\n\r\n## Goal\r\n\r\nHelp beginner users choose the right agency agent automatically, while preserving manual control.\r\n\r\n## Routing Policy\r\n\r\n1. If user explicitly specifies `@agent-name`, treat it as manual override and follow it.\r\n2. If user does not specify an agent, auto-select:\r\n   - architecture / module split / refactor design -> `@software-architect`\r\n   - frontend implementation / component refactor / page performance -> `@frontend-developer`\r\n   - backend API / database / reliability -> `@backend-architect`\r\n   - visual style / UI consistency -> `@ui-designer`\r\n   - interaction flow / information architecture / usability -> `@ux-architect`\r\n   - QA evidence / bug checklist / verification -> `@evidence-collector`\r\n   - release readiness / risk review / final gate -> `@reality-checker`\r\n3. If request spans multiple domains, pick one primary agent and one fallback agent.\r\n4. If request is ambiguous, ask one concise clarification question; otherwise default to `@software-architect`.\r\n\r\n## Response Contract\r\n\r\nWhen an agent is **auto-selected** (user did not specify `@agent`), the reply **must** open with a visible declaration, then continue execution:\r\n\r\n```text\r\n主Agent: @<name> | 备选: @<name> | 理由: <one sentence>\r\n```\r\n\r\nRules:\r\n- This line is mandatory in the answer body — not silent routing.\r\n- Use the exact `@agent-name` form so the user knows which persona is active.\r\n- If the user manually specified `@agent`, acknowledge it: `主Agent: @<user-choice>（手动指定）`\r\n- Then continue execution immediately without waiting for confirmation.\r\n\r\n",
    "cursor-rules-auto-backup": "---\r\ndescription: 新增或更新 Cursor 规则时自动备份到 cursor-rules\r\nalwaysApply: true\r\n---\r\n\r\n# Cursor Rules 自动备份\r\n\r\n## 触发时机\r\n\r\n当在本会话中**新建或更新**以下文件时，必须同步备份：\r\n- 项目规则：`.cursor/rules/*.mdc`\r\n- 全局规则：`C:\\Users\\xwy12\\.cursor\\rules\\*.mdc`\r\n\r\n## 备份目标\r\n\r\n统一备份到中央目录（不存在则创建）：\r\n\r\n`C:\\Users\\xwy12\\Desktop\\my-project\\cursorEnv\\cursor-rules\\latest\\rules\\`\r\n\r\n若同时更新了相关说明文档（如 `agency-agents.md`），备份到：\r\n\r\n`C:\\Users\\xwy12\\Desktop\\my-project\\cursorEnv\\cursor-rules\\latest\\docs\\`\r\n\r\n## 必做动作\r\n\r\n1. 复制文件到对应 `latest/` 子目录（覆盖同名文件）。\r\n2. 全局规则额外保存一份带 `--global` 后缀的副本（例：`foo--global.mdc`）。\r\n3. 在 `cursor-rules/backup-log.md` 追加一条记录：日期、源路径、备份路径、操作类型（新增/更新）。\r\n4. 完成后在回复中简要列出本次备份文件（无需用户额外提醒）。\r\n\r\n## 幂等\r\n\r\n- 重复备份允许覆盖 `latest/` 中的同名文件。\r\n- 不删除历史 `unique/`、`grouped/` 归档，除非用户明确要求。\r\n",
    "graphify": "---\r\ndescription: graphify knowledge graph context\r\nalwaysApply: true\r\n---\r\n\r\nThis project has a graphify knowledge graph at graphify-out/.\r\n\r\n**MANDATORY: Before using Read, Grep, Glob, or Bash to explore the codebase, you MUST run graphify first:**\r\n- `graphify query \"<question>\"` — scoped subgraph for any codebase or architecture question\r\n- `graphify path \"<A>\" \"<B>\"` — dependency path between two symbols\r\n- `graphify explain \"<concept>\"` — all nodes related to a concept\r\n\r\nThis applies to YOU and to every subagent you spawn. Include this rule explicitly in every subagent prompt that involves code exploration. Do not skip graphify because files are \"already known\" or because you are executing a plan — the graph surfaces cross-file dependencies and INFERRED edges that grep and Read cannot find.\r\n\r\nOnly use Read/Grep/Glob directly when:\r\n1. graphify has already oriented you and you need to modify or debug specific lines\r\n2. `graphify-out/graph.json` does not exist yet\r\n\r\n- If `graphify-out/wiki/index.md` exists, navigate it instead of reading raw files\r\n- Read `graphify-out/GRAPH_REPORT.md` only for broad architecture review when query/path/explain do not surface enough context\r\n- After modifying code files, run `graphify update .` to keep the graph current (AST-only, no API cost)\r\n",
    "agentmemory-auto": "---\ndescription: Auto-call agentmemory in every project (recall on start, save decisions)\nalwaysApply: true\n---\n\n# agentmemory Auto (all projects)\n\nMCP server: `user-agentmemory` (mcp.json key `agentmemory`). Do **not** wait for the user to say \"remember\" or \"recall\". This applies in **every** workspace, not only cursorEnv.\n\n## Session start (mandatory, first actions)\n\nBefore exploring or answering from prior context, call:\n\n1. `memory_recall` — `query` = workspace folder name + task keywords; `limit` 8; `format` compact\n2. If recall is thin, `memory_smart_search` with the same query\n3. Incremental Notion → agentmemory sync (rule `notion-agentmemory-sync.mdc`): run `python \"$env:USERPROFILE\\.cursor\\skills\\notion-agentmemory-sync\\scripts\\sync.py\"` unless livez/token unavailable\n\nIf the server is missing / `error` / `needsAuth`, skip silently and continue the task. Do not block work or ask the user to start the service unless they asked about memory.\n\n## During / after work (mandatory)\n\nCall `memory_save` when you learn something useful next session:\n\n- architecture / API / library choices\n- bug root cause and the fix that worked\n- machine-specific paths, ports, env (no secrets)\n- user preferences and \"do it this way\" rules\n\nFields: `content` (1–5 sentences), `type` (`decision` | `pattern` | `preference` | `bug` | `workflow` | `fact`), `concepts` (project slug + topic), `files` if relevant, `project` = workspace folder name (slug, **not** a filesystem path).\n\n## Never save\n\nPasswords, API keys, PATs, tokens, `.env` contents, credentials.\n\n## Subagents\n\nInclude this recall query in subagent prompts that continue the same task. Subagents must `memory_save` their own durable findings.\n",
    "notion-agentmemory-sync": "---\ndescription: Auto-sync Notion updates into agentmemory (all projects)\nalwaysApply: true\n---\n\n# Notion → agentmemory (all projects)\n\nDo **not** wait for the user to say \"sync Notion\". After `memory_recall` at session start, run an **incremental** Notion ingest if agentmemory is up.\n\n```powershell\npython \"$env:USERPROFILE\\.cursor\\skills\\notion-agentmemory-sync\\scripts\\sync.py\"\n```\n\n- Skip silently if `NOTION_TOKEN` is missing or `http://127.0.0.1:3111/agentmemory/livez` fails.\n- If the user created/edited a Notion page this session, run the script again before finishing.\n- Full rebuild only when asked: `python ...\\sync.py --full`\n- Never print `NOTION_TOKEN`. Skill: `notion-agentmemory-sync`.\n",
    "cursor-git-rules": "---\ndescription: Windows 下 Git/GitHub 同步标准流程（含代理与凭据）\nalwaysApply: true\n---\n\n# Cursor Git 同步规则（Windows + GitHub）\n\n当用户要求 **push / 同步到 GitHub / 创建仓库 / 提交并推送** 时，按本规则执行，不先给泛泛方案。\n\n## 环境默认值（本机已验证）\n\n| 项 | 值 |\n|---|---|\n| GitHub 账号 | `wenyuanxu20` |\n| 默认分支 | `main` |\n| 默认可见性 | `private` |\n| SOCKS5 代理 | `socks5://127.0.0.1:7897` |\n| gh 路径 | `C:\\Program Files\\GitHub CLI\\gh.exe` |\n\n## 安全约束（必须遵守）\n\n- **仅在用户明确要求时** 才 `git commit` / `git push`；未明确要求则先确认。\n- **禁止** `git config` 修改、`push --force`（除非用户明确要求）、`reset --hard`、跳过 hooks。\n- **禁止** 提交 `.env`、`*credentials*`、`*.pem`、API Key、授权码、大体积归档（`*.7z`、完整 `venv/`、`node_modules/`）。\n- push 前扫描 `password|api_key|secret|token`；发现硬编码密钥时警告并建议改为环境变量。\n- **禁止** 在回复中输出完整 token；凭据只用于命令，不写入文件。\n\n---\n\n## A. 日常 push（已有仓库）\n\n### 1. 并行检查\n\n```powershell\ngit status --short\ngit diff --stat\ngit log -8 --oneline\ngit remote -v\n```\n\n### 2. 提交（用户已要求 push 时）\n\n```powershell\ngit add -A\n$msg = @'\n<标题：动词开头，说明 why>\n\n<可选正文：1-2 句补充上下文>\n'@\ngit commit -m \"$msg\"\n```\n\n- 提交信息聚焦 **why**，与仓库近期风格一致。\n- PowerShell 多行消息用 `$msg = @' ... '@`；**不要** 用 bash heredoc。\n\n### 3. 设置代理并 push\n\n```powershell\n$env:Path = \"C:\\Program Files\\GitHub CLI;\" + $env:Path\n$env:ALL_PROXY = \"socks5://127.0.0.1:7897\"\n$env:HTTPS_PROXY = \"socks5://127.0.0.1:7897\"\n\ngit push origin main\n```\n\n### 4. 非交互凭据 fallback（push 卡住 / 提示输入用户名密码时）\n\n```powershell\n$credOutput = \"protocol=https`nhost=github.com`n\" |\n  git -c credential.https://github.com.helper= `\n      -c credential.https://github.com.helper=manager credential fill 2>$null\n$username = ($credOutput | Select-String '^username=(.+)$').Matches.Groups[1].Value\n$token    = ($credOutput | Select-String '^password=(.+)$').Matches.Groups[1].Value\n$pair     = \"$username`:$token\"\n$b64      = [Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes($pair))\ngit -c credential.helper= `\n    -c http.https://github.com/.extraheader=\"AUTHORIZATION: basic $b64\" `\n    push origin main\n```\n\n### 5. 验证\n\n```powershell\ngit status -sb\ngit rev-parse HEAD; git rev-parse origin/main   # 两者应一致\n```\n\n---\n\n## B. 新建仓库并首次 push\n\n### 1. 前置\n\n- 无 `.git` → `git init` + `git branch -M main`\n- 无 `.gitignore` → 按项目类型创建（见下方模板）\n- 检查大文件与密钥后再 `git add .`\n\n### 2. 创建远程并推送\n\n```powershell\n$env:Path = \"C:\\Program Files\\GitHub CLI;\" + $env:Path\n$env:ALL_PROXY = \"socks5://127.0.0.1:7897\"\n$env:HTTPS_PROXY = \"socks5://127.0.0.1:7897\"\n\n$credOutput = \"protocol=https`nhost=github.com`n\" | git credential fill 2>$null\n$env:GH_TOKEN = ($credOutput | Select-String '^password=(.+)$').Matches.Groups[1].Value\ngh auth setup-git 2>$null\n\n$Owner = \"wenyuanxu20\"\n$RepoName = \"<仓库名>\"\n\n$view = gh repo view \"$Owner/$RepoName\" 2>&1\nif ($LASTEXITCODE -ne 0) {\n  gh repo create $RepoName --private --source=. --remote=origin `\n    --description \"<简短描述>\"\n} else {\n  git remote remove origin 2>$null\n  git remote add origin \"https://github.com/$Owner/$RepoName.git\"\n}\n\ngit push -u origin main\n# 若卡住，使用 A.4 的 extraheader fallback\n```\n\n### 3. 完成后输出\n\n- 仓库 URL、分支、最新 commit hash\n- 是否排除了敏感/大文件\n- 若发现硬编码密钥，给出 remediation 建议\n\n---\n\n## C. `.gitignore` 最小模板\n\n**Python 项目：**\n```\nvenv/  __pycache__/  *.py[cod]  build/  dist/\n.env  .env.*  *.log\ngraphify-out/cache/\n.DS_Store  Thumbs.db  .idea/  .vscode/\n```\n\n**Node / 全栈：**\n```\nnode_modules/  dist/  .venv/  __pycache__/\n.env  .env.local  *.7z  *.log\ngraphify-out/cache/\n```\n\n**通用文档仓库：**\n```\n.env  .env.*  *.pem  credentials.json\n.DS_Store  Thumbs.db  .vscode/  .idea/\n```\n\n---\n\n## D. 网络与凭据排障\n\n按顺序诊断，**不要** 在 global git config 长期写入 proxy。\n\n| 现象 | 处理 |\n|------|------|\n| `443` 超时 / `Connection reset` | 设置 `$env:ALL_PROXY = \"socks5://127.0.0.1:7897\"` 后重试 |\n| HTTP 代理可用但 git 报 SOCKS 不支持 | **不要** 用 `git config http.proxy socks5://...`；改用环境变量 + A.4 extraheader |\n| `gh` 未找到 | `winget install GitHub.cli`，PATH 加 `C:\\Program Files\\GitHub CLI` |\n| `gh auth` 失败 | 从 GCM 取 token 设 `$env:GH_TOKEN`，再 `gh auth setup-git` |\n| SSH `Permission denied (publickey)` | 改回 HTTPS remote，优先 A.4 方案；SSH 需用户手动在 GitHub 添加公钥 |\n| push 等待用户名密码 | 立即用 A.4 非交互 push，不要挂起等待 |\n| `git push` 长时间无响应 | 终止进程，设代理 + extraheader 重试 |\n\n**代理探测（可选）：**\n```powershell\n$env:ALL_PROXY = \"socks5://127.0.0.1:7897\"\ngh api user -q .login    # 返回用户名则代理+凭据正常\n```\n\n---\n\n## E. Agent 执行清单\n\n用户说「push 到 git / 同步 GitHub / 创建仓库」时：\n\n1. 进入目标目录，执行 A.1 检查\n2. 若无 `.gitignore` 或含 secrets → 先处理再继续\n3. 用户已授权则 commit（A.2）\n4. 设代理 → push（A.3）→ 失败则 A.4 fallback\n5. 验证并汇报：URL、commit、是否完全同步\n6. 若 push 后文件又被进程改写（如实时数据），告知用户并询问是否再提交\n\n**PowerShell 注意：** 避免 `&&` 链式命令；用 `Set-Location` + 分号或分开执行。\n\n---\n\n## F. 复制到其他项目\n\n将本文件复制到目标项目的 `.cursor/rules/cursor-git-rules.mdc` 即可。可按项目修改：\n\n- `$Owner` / 默认 remote\n- 代理地址（若本机端口不同）\n- `.gitignore` 模板段落\n",
    "novel-wiki-bootstrap": "---\ndescription: 中文触发“构建小说wiki”自动搭建长篇连续性知识库\nalwaysApply: true\n---\n\n# 小说 Wiki 自动构建\n\n## 触发词\n\n当用户输入以下任一短语时，立即按本规则执行，不先给方案：\n\n- `构建小说wiki`\n- `构建 小说 wiki`\n- `搭建小说知识库`\n- `建立小说wiki`\n- `build novel wiki`\n\n## 模板源（按顺序找，用第一个存在的）\n\n1. `%USERPROFILE%\\.cursor\\skills\\novel-continuity\\templates\\`\n2. `%USERPROFILE%\\.agents\\skills\\novel-continuity\\templates\\`\n3. `C:\\Users\\xwy12\\Desktop\\my-project\\cursorEnv\\skills\\novel-continuity\\templates\\`\n\n项目级规则源（同技能 `rules/novel-continuity.mdc`）：\n\n1. `%USERPROFILE%\\.cursor\\skills\\novel-continuity\\rules\\novel-continuity.mdc`\n2. cursorEnv 同上相对路径\n\n## 必做动作\n\n1. 在**当前工作区根目录**创建（不存在才建）：\n   - `manuscript/`\n   - `wiki/novel/`\n   - `raw/novel/`\n   - `wiki/log.md`（若无）\n   - `AGENTS.md`（若无则最小文件，再追加小说章节）\n\n2. 从模板源复制（**目标已有非空文件则跳过，不覆盖**）：\n\n   | 模板 | 目标 |\n   |------|------|\n   | `wiki-index.md` | `wiki/novel/index.md` |\n   | `00-continuity-now.md` | `wiki/novel/00-continuity-now.md` |\n   | `01-characters.md` | `wiki/novel/01-characters.md` |\n   | `02-timeline.md` | `wiki/novel/02-timeline.md` |\n   | `03-foreshadowing.md` | `wiki/novel/03-foreshadowing.md` |\n   | `04-rejected.md` | `wiki/novel/04-rejected.md` |\n   | `05-voice.md` | `wiki/novel/05-voice.md` |\n   | `memory-policy.md` | `wiki/novel/memory-policy.md` |\n   | `manuscript-readme.md` | `manuscript/README.md` |\n\n3. 将 `rules/novel-continuity.mdc` 复制到当前项目 `.cursor/rules/novel-continuity.mdc`（已存在非空则跳过）。\n\n4. 若 `AGENTS.md` 尚无「小说连续性」段落：把 `AGENTS-novel-section.md` 追加到文件末尾。\n\n5. 在 `wiki/log.md` 顶部追加一条今日记录：小说 wiki 脚手架。\n\n6. 幂等：不覆盖用户已有非空内容；不删除 `manuscript/` 已有章节。\n\n7. 若 `raw/novel/` 已有资料：按模板把可确认事实 ingest 进 `wiki/novel/` 对应页。若为空：请用户提供书名、POV、已有人物，或把设定草稿放入 `raw/novel/`。\n\n## 输出格式\n\n执行后必须输出：\n\n- 创建 / 跳过文件清单\n- 下一步：填 `05-voice.md` 与 `01-characters.md`，再写 `manuscript/ch-001.md`\n- 提醒：续写读 `00-continuity-now.md`；agentmemory 只记铁律（见 `memory-policy.md`）\n"
};
