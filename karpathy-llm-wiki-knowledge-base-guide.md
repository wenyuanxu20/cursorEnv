# Karpathy LLM Wiki 知识库搭建指南

## 1. 这是什么

`LLM Wiki` 是 Karpathy 提出的知识库方法：  
不是每次提问都从原始资料临时检索（传统 RAG 思路），而是让 LLM 持续维护一个可演进的 Wiki 中间层。

一句话理解：**把知识“先编译成 Wiki”，再在 Wiki 上问答和迭代。**

参考原文：<https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f>

---

## 2. 核心架构（3 层）

```text
raw/      # 原始资料层：只增不改，保留事实源
wiki/     # 知识层：LLM 维护的结构化页面
schema/   # 规则层：告诉 LLM 如何写、如何更新、如何校验
```

推荐再加两个关键文件：

- `wiki/index.md`：Wiki 总目录（按主题分组）
- `wiki/log.md`：每次 ingest 的变更记录

---

## 3. 为什么适合做长期知识库

- **知识会累积**：新资料进入后，LLM 直接更新已有页面，不是重复从零总结
- **结构更稳定**：主题页、概念页、实体页会逐步形成统一术语体系
- **冲突可追踪**：新旧观点冲突时可在 Wiki 中显式标注
- **查询成本更低**：先读 `index.md` 和相关主题页，再做综合回答

---

## 4. 最小可用目录（建议直接创建）

```text
knowledge-base/
  raw/
  wiki/
    index.md
    log.md
  schema/
  AGENTS.md
```

> 如果你就在当前仓库做，也可以直接把这些目录建在项目根目录。

---

## 5. AGENTS.md 应写什么（最关键）

`AGENTS.md` 是整个系统的“操作系统”。至少包含以下规则：

1. **页面模板统一**
   - 定义
   - 关键结论
   - 证据与来源
   - 争议/不确定项
   - 相关页面

2. **来源强约束**
   - 每个关键结论必须引用 `raw/` 中具体来源
   - 区分“来源事实”与“LLM 推断”

3. **更新策略**
   - 优先更新已有页面，避免重复建同义页面
   - 新建页面必须补齐双向链接

4. **冲突处理**
   - 新旧资料冲突时，保留两方观点并注明证据与时间

5. **命名规范**
   - 固定术语、统一页面命名、避免同义词碎片化

---

## 6. 三个固定工作流

## 6.1 Ingest（新增资料）

目标：把一份新资料“编译”进 Wiki。

步骤：

1. 阅读 `raw/` 新资料
2. 找到相关旧页面并更新（必要时新建）
3. 在页面中新增结论与来源引用
4. 更新 `wiki/index.md`
5. 记录 `wiki/log.md`（日期、修改页面、冲突点）

---

## 6.2 Query（回答问题）

目标：基于 Wiki 进行稳定回答，而非仅靠临时检索。

步骤：

1. 先读 `wiki/index.md` 定位主题页
2. 再读相关页面做综合
3. 输出时标注引用页面
4. 若发现知识空洞，将高价值问答回写 Wiki

---

## 6.3 Lint（健康检查）

目标：让知识库长期可用、可维护。

检查项：

- 孤儿页面（无入链/出链）
- 重复页面（同义不同名）
- 无来源结论
- 冲突未标注
- 长期未更新且可能过时的页面

建议频率：每周 1 次。

---

## 7. 可直接复用的提示词

### 7.1 Ingest 提示词

```text
请执行一次 LLM Wiki ingest：
1) 阅读 raw/ 下新增资料：<文件名>
2) 更新 wiki/ 中相关页面；必要时新建页面
3) 每个关键结论都附来源（raw/<文件名>）
4) 更新 wiki/index.md（分类、摘要、来源数）
5) 在 wiki/log.md 记录本次变更（日期、页面、冲突点、待办）
```

### 7.2 Query 提示词

```text
基于 wiki/ 回答问题：<你的问题>
要求：
- 先从 wiki/index.md 定位相关页面，再综合回答
- 每条关键结论标注出处页面
- 对证据不足部分明确标注“不确定”
- 给出建议补充的 raw 来源类型
```

### 7.3 Lint 提示词

```text
请对 wiki/ 做健康检查并输出修复清单：
- 孤儿页面
- 重复术语/重复页面
- 无来源结论
- 冲突未标注的段落
- 过时风险页面
按优先级排序并给出修复步骤。
```

---

## 8. 落地节奏（第一周建议）

- Day 1：建目录 + 完成 `AGENTS.md` 初稿
- Day 2-3：导入 10-20 份高质量资料，执行 3-5 次 ingest
- Day 4：统一命名和链接，修复明显重复页面
- Day 5：做第一次 lint，补充规则到 `AGENTS.md`

关键目标不是“覆盖全部知识”，而是先把流程跑顺、规则跑稳。

---

## 9. 常见误区

- 只做检索，不做页面更新（无法形成复利）
- 只建新页，不维护旧页（很快碎片化）
- 页面没有来源（后期不可验证）
- 不记录变更日志（无法追踪认知演化）
- 不做定期 lint（Wiki 会逐步失真）

---

## 10. 结论

Karpathy LLM Wiki 适合长期学习和研究型知识管理。  
你的重点应放在三件事：

1. 建立稳定结构（raw/wiki/schema）
2. 固化流程（ingest/query/lint）
3. 持续打磨规则（AGENTS.md）

这样知识库会从“文件堆积”变成“可演进的知识系统”。

