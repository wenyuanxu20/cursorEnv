[project]
name = "cursorenv-scrapling"
version = "0.1.0"
description = "Local Scrapling runtime for cursorEnv/getInfo (pinned PyPI package)."
requires-python = ">=3.10,<3.14"
dependencies = [
  "scrapling[all]==0.4.15",
]

[tool.uv]
package = false
